{
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  },
  "name": "restaurant-go",
  "description": "Restaurant Go is a restaurant website built with Tailwind CSS.",
  "version": "1.0.0",
  "main": "tailwind.config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx tailwindcss -i src/input.css -o src/output.css --watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.2",
    "flowbite": "^2.2.1"
  }
}
